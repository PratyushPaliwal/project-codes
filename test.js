console.log("this is test file");
const var = [5,4,3,2];
console.log("var array is: " + var);

if(var.length === 4 ){
  console.length("length of array is" + var.length);
}
else
  console.length("Length is certainly not 4");

